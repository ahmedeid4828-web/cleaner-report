            });
        }
        function exportToCSV() {
            if (reports.length === 0) {
                alert('No data to export!');
                return;
            }
            // Create CSV content
            let csvContent = 'Company Name,Problem Type,Employee Name,Country,Order Number,Date\n';
            reports.forEach(report => {
                csvContent += `${report.companyName},${report.problemType},${report.employeeName},${report.country},${report.orderNumber},${report.date}\n`;
            });
            // Download CSV file
            const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
            const link = document.createElement('a');
            const url = URL.createObjectURL(blob);
            link.setAttribute('href', url);
            link.setAttribute('download', 'cleaner_reports.csv');
            link.style.visibility = 'hidden';
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }
    </script>
</body>
</html>

الفكرة الأساسية
إنت عاوز نموذج إدخال بيانات (Form) على صفحة HTML:
-	بعض الخانات بتكتب فيها يدوي (كوبي/بيست).
-	بعض الخانات بتكون منسدلة (Drop-down).
-	زرار في الآخر يعمل Export للبيانات اللي اتسجلت.

⸻

الخانات اللي هتكون موجودة
اسم الشركة → (خانة نص مفتوحة تكتب فيها).
نوع المشكلة → (قائمة منسدلة فيها: Quality issue, Slow, Hygiene issue, Appearance, Bad attitude).
اسم العاملة → (خانة نص مفتوحة تكتب فيها).
الدولة → (قائمة منسدلة فيها: الكويت، البحرين، السعودية، قطر، عمان، الإمارات).
رقم الطلب → (خانة نص مفتوحة/أرقام).
التاريخ → (خانة تاريخ تختار منه من الكالندر).
⸻

كيف هيشتغل النموذج
كل مرة تملأ الخانات وتضغط Submit/إضافة → البيانات تتحفظ في جدول تحت النموذج.
الجدول ده بيكبر مع كل إدخال جديد.
في أي وقت تضغط على زرار Export → كل الجدول يتصدر كملف Excel/CSV وتقدر تحلله بعدين (مين العاملة – نوع المشكلة – الدولة … إلخ).
⸻

النتيجة اللي هتحصل عليها
واجهة بسيطة: نموذج فوق + جدول تحت + زرار تصدير.
إدخالك للبيانات هيكون يدوي وسهل.
التصدير هيخليك تاخد كل الداتا مرة واحدة وتفتحها في Excel أو Google Sheets وتشتغل عليها بالفلترة
اسم الشركه يكون
Justclean company
Header يكون cleaner low productivity

الالوان بربل مع ابيض

عاوز النموزج بالانجليش

Copy message
Edit message
BLACKBOXAI

html
207 lines
Copy code
Download code
Click to close
<!DOCTYPE html>
<html lang="en">
...
Explanation:
Header: Set to "Cleaner Low Productivity" as requested.
Form Fields: All in English, with the company name pre-filled as "Justclean company". Dropdowns match the specified options (countries translated to English for consistency).
Functionality:
Submitting the form adds the data to a table below without page reload.
The table grows with each entry.
Export button generates and downloads a CSV file (which can be opened in Excel or Google Sheets for filtering/analysis).
Styling: Purple background (#8A2BE2) with white text. Form and table have semi-transparent white overlays for readability. Buttons are white with purple text.
Usage: Save this as an .html file and open in a browser. Data is stored in memory (resets on page refresh), but you can extend it to localStorage if needed.
If you need modifications, let me know!



Copy message
Export
START TRIAL NOW
Upgrade plan




Chat

Landing Page

Web App

AI Apps

Game Dev

Image Gen

Upload
Models